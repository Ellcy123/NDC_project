# Verified together on Windows x64 / Python 3.11; see dependencies.md.
# Install exactly one OpenCV distribution in the dedicated virtual environment.
Pillow==12.1.1
numpy==2.4.6
opencv-python==5.0.0.93
