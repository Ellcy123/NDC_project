# 美术工作区整理记录

日期：2026-09-05。用户已确认执行，并于同日授权提交、推送本轮整理改动。实际发布结果以 Git 提交历史为准。

后续状态：用户暂停发布，等待同事提交。已于本日安全拉取 `fa0b11c`（表情提交v1）；用户随后授权整理其中的文件命名和配对问题，删除 `simplify-ndc-emergency-art`、`ndc-storyboard-to-seedance` 与旧 `runninghub-h3-ref2va`，并确认三个现行 H3 视频 Skill 迁回工程维护。该阶段本地整理尚未提交或推送；用户随后确认可移植路径与脚本入 Git，并于本日恢复提交、推送授权。

## 整理后的入口

| 内容 | 位置与结果 |
|---|---|
| 静态美术 Skill | 策划仓库 `.codex/skills`；14 个主实现，其中 4 个道具目录保留同事原版 |
| H3 视频 Skill | 工程仓库 `.codex/skills`；生成审查、双音频调用、确认后交付处理共 3 个主实现 |
| 其他项目 / 个人入口 | 保留 11 个直接指向对应主实现的兼容入口；三个删除项退出发现目录，原版有哈希备份 |
| 角色库 | `美术资产交付/角色/人物/<人物>/cards`；42 人、63 张角色卡、一个 JSON 索引 |
| 新生产任务 | `{WORK_ROOT}/jobs/<任务>/payload`；候选、修改、检查图和待确认成品统一存放 |
| 正式交付 | 用户选定精确版本后，按工程目标复制并校验哈希；工具不会自行覆盖工程资源 |
| 任务收尾 | 交付验证完成或用户明确取消后清理临时大文件；保留小型记录及必要正式母版 |

完整使用流程见 [美术生产工作区](../../docs/美术生产工作区.md)，角色使用规则见 [角色库说明](../../美术资产交付/角色/README.md)。

## 角色卡与旧文件

新增提交中的 61 张 PNG 已按人物归位并核验原始字节，另收录旧库 Margaret 深发版 2 张。不同色版、Margaret 发色版本和 Pierce 两版分别保留；入库不等于选定，所有导入卡片均未冒充用户批准。

原 `image/角色卡666` 的 41 个图像文件保留：40 张仍作为历史兼容来源登记，1 张男性角色图身份待核实。历史交接文件继续引用它们，不批量改写旧任务证据。工程内 `D:/NDC/NDC_project`、旧多角色生产区及正在产出的 Emma 视频预览均未清理。

本轮没有改 Unity 正式图片、`.meta`、运行时表或其他任务的剧情文档，也没有调用付费生成 API。

## 与道具 Skill 的协作边界

基于已拉取的 `51ca823507497e14df5ca419c6d54bb658807c1f`。以下四个目录保持原字节，本轮只在公共规则和独立兼容记录中接入路径与清理要求：

- `ndc-scene-evidence-placement`
- `ndc-evidence-scene-placement`
- `ndc-evidence-container`
- `ndc-evidence-detail-art`

运行 `python -B scripts/art_pipeline/check_upstream.py --fetch` 可查看后续远程改动、与本地文件的重叠及需复核的道具合同。当前没有文本重叠，不代表未来提交必然兼容。

已有 3 个旧道具子流程与新主流程的参数、遮罩和交付合同不一致，已标记不可独立执行旧命令；等待同事版本稳定后定向同步。旧机器的 `D:/PMH/美术` 和 `D:/Codex/NDC/NDC风格化角色素材库` 本机缺失，涉及这些参考的生成仍需确认有效原图。

## 验证与回退

### 新增表情资源归档（fa0b11c）

15 位人物的 199 张原图完整保留：99 对绿幕/透明表情和 1 张 Emma 参考底图。13 个重复 `.png.png` 后缀修正；绿幕 `emma表情`、`zack表情` 与透明侧统一为 `emma`、`zack`；参考底图独立存放。共 27 张图片地址变化，均与同事提交的 Git blob 和 SHA-256 核验一致，不重编码、不改图。

新增 `美术资产交付/角色表情/表情索引.json`，记录人物关联、旧地址、新地址、表情配对、尺寸和来源哈希；所有条目保持未确认。两库未检出真实消费者引用这些旧地址；异常名称测试夹具保留。Earl 表情通过人物档案关联，卡库缺卡如实记录，不虚构对应角色卡。

路径迁移的小型清单位于 `D:/NDC_Art_Work/_migration_backups/20260905_expression_naming_fa0b11c/path_and_hash_manifest.json`，未复制额外图片。

- 角色索引检查：42 人、63 资产、102 来源记录；0 错误、0 警告。
- 当前 13 个已接入主 Skill 已通过格式校验（策划 10、工程 3）；另 4 个道具主实现保持同事原版。其他位置保留的 11 个兼容入口已逐项核对，直接指向登记的唯一主实现，无跳转循环。
- 人物入景工具：29 项测试通过，覆盖真实受管任务路径。
- 工作区生命周期与视觉记录校验器：23 项测试中 22 项通过；1 项符号链接测试因本机权限跳过。
- 独立复核确认：真实任务路径可接入；清理中断可安全重试且保护新增文件；远程检查能识别受保护目录中的新增、暂存与重命名。
- Dashboard 按维护归属列出 17 个美术/视频主入口（策划 14、工程 3），合计 47 个条目；删除项不会被刷新脚本重新加入，生成与脚本语法检查通过。
- 两库本轮修改通过 `git diff --check`；角色锁定提示词保持不变。

旧 Skill 迁移备份位于 `D:/NDC_Art_Work/_migration_backups/20260905_skill_consolidation`，共 33,122,689 字节，约 31.6 MiB，带原文件哈希清单。这是本次整理的可回退副本，不属于自动清理的出图任务。受管出图任务默认总预算 50 GiB；预算超限会阻止新建，不能替代操作系统磁盘配额。

用户删除的两个静态/分镜 Skill 当前版本及工程入口已移出发现目录，保存在 `D:/NDC_Art_Work/_migration_backups/20260905_user_removed_skills`，共 14 个文件逐一核验原哈希。随后确认的视频调整使用独立备份 `D:/NDC_Art_Work/_migration_backups/20260905_video_skills_to_engine`，保留迁移当时最新的 H3 实现和旧入口。生产素材、等待确认中的视频任务及共享项目外工作区未迁位或删除。

视频迁移验收：三个主实现与迁移前当前策划版本逐字节一致；六个策划/个人兼容入口及原 UI 配置已核对；四个脚本帮助入口通过，audio 缺少输出目录时在网络调用前拒绝；外部短视频夹具验证 staging 干运行不修改输入、不创建 staging、不改变工程目标，夹具已清理。完整 36 文件映射和验证结果见该备份目录的 `migration_result.json`，原哈希见 `hash_manifest.json`，备份共 321,065 字节。

## 跨机器路径与脚本入库（后续用户确认）

用户明确：两库不要求固定盘符、文件夹名或父子/相邻关系；Skill 使用的自有脚本必须跟随 Git。已采用两库均可调用的 `scripts/art_pipeline/ndc_art.py`，机器路径另存被忽略的 `ndc.local.json`，共享策略和 Skill 登记只保留归属与相对路径。依赖说明与 Python 清单位于同目录 `dependencies.md` / `requirements.txt`。文中迁移备份等绝对地址是这次整理的历史证据，不是新电脑的运行前提。

公共入口还负责保护道具打包的受管输出位置；四个同事维护目录继续保持原字节。旧外部批准素材仍需找回或明确指定有效替代版本，路径配置不能补出缺失的原图。

本轮可移植性验收：两库入口在中文、空格、不相邻目录的临时副本中通过配置、定位、脚本调用与工作区生命周期测试；未配置时帮助可用，执行给出明确配置提示。共享测试 28 项中 27 项通过、1 项符号链接测试因本机权限跳过；锁定 Python 依赖版本下入景 29 项、表情 7 项现有测试通过。四个同事道具目录、锁定提示词及原 UI YAML 哈希不变。47 条看板记录全部使用仓库归属和相对路径。角色卡 42 人/63 张、表情 199 张复核通过。

工程库曾遗留一个自本日 09:26:39 起未变化的零字节 Git index.lock；确认没有运行中 Git 进程后，仅移除该旧锁以恢复新增源码的 Git 跟踪。没有执行提交、推送或清理其他任务。

## 发布前同步

用户于 2026-09-05 恢复本轮提交、推送授权。发布前策划库安全快进至 `43c895e`（Unit5 文档同步），工程库安全快进至 `d49c0cba7`；两库远程改动均与全部本地改动无文件重叠，已有本地文件字节与暂存差异保持不变。发布范围限本轮 Skill 归属、可移植工具、工作区规则、角色卡/表情目录与看板；其他任务的剧情、正式资源、配置表、语音和视频过程文件保留本地。实际提交和推送结果以 Git 历史为准。
