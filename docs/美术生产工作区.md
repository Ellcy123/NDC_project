# 美术生产工作区

用户于 2026-09-05 确认最终分工：策划维护静态美术 Skill、角色卡与表情参考；工程维护 H3 视频生成、调用和交付 Skill；图片与视频均在项目外生产，用户确认具体成品后交工程，任务结束清理临时大文件。

仓库用“策划 / 工程”两个角色定位，目录名、盘符和两库相对位置均不固定。本文中的 `{PLANNING_ROOT}`、`{ENGINE_ROOT}`、`{WORK_ROOT}`、`{DELIVERY_ROOT}` 是路径查询结果的占位符，使用前必须替换为实际值，不能当作字面文件名执行。

| 内容 | 唯一入口 |
|---|---|
| 静态美术 Skill 主实现 | 策划仓库 `.codex/skills/`，实际清单以注册表为准 |
| H3 视频 Skill 主实现（3 个） | 工程仓库 `.codex/skills/` |
| 角色卡和人物/版本索引 | 策划仓库 `美术资产交付/角色/角色索引.json` |
| 长期表情参考和绿幕/透明配对 | 策划仓库 `美术资产交付/角色表情/表情索引.json` |
| 自有脚本、模板、规则及依赖清单 | 对应仓库的 Git 文件；从主 Skill 目录解析支持文件 |
| 本机绝对路径 | 两库根目录各自的 `ndc.local.json`，被 Git 忽略 |
| 团队空间预算及相对资源地址 | 策划仓库 `production/art_pipeline/paths.json`，随 Git 管理 |
| 新任务临时工作区 | `{WORK_ROOT}/jobs/<任务>/payload/`，必须在两仓库外 |
| 正式用户交付 | `{DELIVERY_ROOT}`，目录名固定为 `最终交付`，必须与只读工程根、过程根分离 |
| 工程内仍在使用的旧生产区 | 工程仓库内 `NDC_project/`，按原任务保留 |

工程内旧生产目录不是独立策划仓库，不能整目录覆盖或删除。历史记录中的 `D:/NDC_project`、`D:/NDC` 等只是当时机器位置，不作为团队默认路径或目录识别依据。

## 新电脑首次配置

先拉取两个仓库，按照 [脚本依赖说明](../production/art_pipeline/dependencies.md) 准备 Python 环境。项目自有脚本必须连同 Skill 进 Git；不要把个人工具目录、临时工作区中的脚本或个人 Python 安装路径写成团队运行前提。Python/FFmpeg 等第三方软件按依赖说明安装，虚拟环境、密钥和过程文件不进 Git。

从任一仓库根目录执行一次配置。以下三个地址只是示例，请替换为自己电脑上的真实位置；工作区可选空间充裕的任意磁盘，不要求 C 盘或 D 盘，也不要求两个仓库相邻。

```powershell
python -B scripts/art_pipeline/ndc_art.py configure --planning-root "E:/Design Work/Story" --engine-root "F:/Game/Client" --work-root "G:/Art Scratch"
python -B scripts/art_pipeline/ndc_art.py paths
python -B scripts/art_pipeline/ndc_art.py preflight expressions
python -B scripts/art_pipeline/ndc_art.py preflight style-check
python -B scripts/art_pipeline/ndc_art.py skill ndc-generate-characters
```

配置命令在两库写入被忽略的 `ndc.local.json`；Git 内仅提供 `ndc.local.example.json` 格式示例。移动任一仓库后重新配置。没有配置时应报出缺少的字段，不能猜测隔壁文件夹或回退到其他人的 D 盘。首次配置也可通过 `--delivery-root` 指定另一个名为 `最终交付` 的目录；表情和风格库由同一命令的对应可选参数登记，未登记时预检只提示用户指定，不替代来源。

两库都带 `scripts/art_pipeline/ndc_art.py`：策划库保存公共实现，工程库保存可随 Git 获取的转接脚本。所有后续命令从当前仓库根目录运行；`skill <名称>` 返回主 `SKILL.md` 及 `skill_root`，`run <名称> <脚本名> ...` 从主 Skill 的 `scripts/` 执行。兼容入口和个人入口只负责定位，不存另一份实现，也不是同事执行所必需的依赖。

被 Git 忽略的 `ndc.local.json` 独立保存每台设备的 `planning_root`、`engine_root`、`work_root`、`delivery_root`、表情前置资产和三套完整风格自检库。共享 Skill 不保存这些盘符。新设备执行 `configure` 时，若未传 `--delivery-root`，会在策划 checkout 的上级 Codex 工作根创建同名 `最终交付`；表情和自检库不得自动猜测，分别由 `preflight expressions`、`preflight style-check` 在首次使用时提示用户指定。

环境变量 `NDC_PLANNING_ROOT`、`NDC_ENGINE_ROOT`、`NDC_ART_WORK_ROOT`、`NDC_ART_DELIVERY_ROOT` 可供脚本会话和自动化临时覆盖；若它们与当前 checkout 的 `ndc.local.json` 不一致，入口会拒绝静默继续。只有明确知道正在临时切换一整组 checkout 时才可在该会话设置 `NDC_ART_ALLOW_CONFIG_ROOT_OVERRIDE=1`，随后应重新运行 `configure` 固化正确配对。旧 `NDC_ART_PATHS_CONFIG` 只为已有任务/测试显式兼容，新电脑走上述入口。锁定提示词和已完成任务日志不嵌入新机器地址。

## 每次出图

1. 从角色索引确定人物、色版和精确版本。索引的 `unconfirmed` 不是美术批准；使用用户明确指定的版本或可核实批准的原图，不根据文件名、最新日期或提交名推定批准。
2. 在正式 Skill 开始前创建任务。JSON 输出中的 `output` 是这次所有新候选、修图、审核证据和待确认成品的共同根。必要时在其下分目录，不能继续往两个仓库或系统临时目录堆积新过程图。
3. 按具体 Skill 完成艺术和技术检查。通过自检后用 `ready` 标记等待用户，展示具体成品。自检通过不代表用户已经选定。
4. 用户确认具体字节版本后，记录 `approve`。若确认后继续改图，原选择失效，回到 `resume`，再次展示新成品。
5. 在用户已经授权的工程目标目录交付；沿用工程命名、格式、`.meta` 和正式表工作流。此工具不会自动复制或覆盖 Unity 文件。完成实际复制后，用 `record-copy` 校验源和目的 SHA-256。
6. 在任务关闭前，给必须长期保留的高清/可编辑母版标记 `mark-master`，把它独立保存到正式素材库，再登记 `record-copy --kind master`。临时工作区内另一份副本、硬链接都不算长期保存。
7. 成功交付或用户明确放弃后 `close`。工具验证留存副本，然后立即清理该受管任务的 `payload`，仅留小型任务记录、选择/交付哈希和关闭原因。

```powershell
python -B scripts/art_pipeline/ndc_art.py workspace create --name Zack_站姿 --kind character
python -B scripts/art_pipeline/ndc_art.py workspace ready --job <输出的job路径>
python -B scripts/art_pipeline/ndc_art.py workspace approve --job <job> --asset final/zack.png --note "用户确认的具体消息或选择"
# 按已授权目标完成复制后登记；本命令不进行复制：
python -B scripts/art_pipeline/ndc_art.py workspace record-copy --job <job> --source final/zack.png --destination <工程内实际文件> --kind delivery
python -B scripts/art_pipeline/ndc_art.py workspace close --job <job> --result delivered --note "已确认并校验工程交付"
```

有母版时，在 `close` 前使用：

```powershell
python -B scripts/art_pipeline/ndc_art.py workspace mark-master --job <job> --source masters/zack.psd
python -B scripts/art_pipeline/ndc_art.py workspace record-copy --job <job> --source masters/zack.psd --destination <项目外工作区以外的正式母版文件> --kind master
```

失败但仍要返工、被工具故障阻塞、等待用户回复，都属于未结束任务；不得自动归为取消。用户明确取消后才能用 `close --result cancelled --note <用户取消依据>`。关闭后的任务不再就地续写，重新创建任务并引用正式母版和旧任务记录。

## 空间管理

工作区默认总预算 50 GiB，新建任务前至少保留磁盘 10 GiB 可用空间。新建任务先清理已结束且可验证的受管任务；若空间仍不足，停止启动新批次。每个大批次或生成阶段开始前再次查看 `status`，不要把预算检查当成磁盘配额隔离。工具不会终止正在生成的文件，也不能阻止绕过工具直接写盘。

```powershell
python -B scripts/art_pipeline/ndc_art.py workspace status
python -B scripts/art_pipeline/ndc_art.py workspace cleanup
python -B scripts/art_pipeline/ndc_art.py workspace cleanup --apply
```

`cleanup` 默认只预览；`--apply` 也只处理状态已经关闭且有合法记录的 `jobs/<任务>/payload`。工作中、待确认、来源不明、旧任务、迁移备份、角色卡和工程资源一律不进入自动清理。目录链接/Junction、异常文件或失效的交付副本会阻止清理。不要拿一个宽泛的 `output` 或 `Art` 文件夹直接登记为已结束任务。

已有的 `output/multichar_avg`、`image/edit_jobs`、工程 `Art/runninghub` 等含活跃制作。它们保留原地址和原始哈希，不改历史交接文件。后续逐任务确认关闭状态、保留必要母版并验证消费者后再清理，不通过批量改历史引用伪造“已迁移”。

## Skill 与同事并行修改

每个 Skill 只维护一份完整实现，主目录和维护归属登记在 `production/art_pipeline/skill_sources.json`：静态美术 Skill 位于策划库，H3 视频 Skill 位于工程库，数量随注册表维护。2026-09-11 起新建并提交的 NDC Skill 必须采用以下结构；五项均为必需且在提交时含真实、与职责有关的内容，不能用空目录冒充。UI 元数据需要时可另加 `agents/`，但不替代五项核心结构。

```text
skill-name/
├── SKILL.md
├── references/  # 规范、说明、案例
├── assets/      # 参考图、模板、蒙版、色板
├── scripts/     # 执行脚本
└── tests/       # 测试
```

`SKILL.md` 负责入口和路由，详细规范/案例进入 `references/`；Skill 专属参考图、模板、蒙版和色板进入 `assets/` 并从当前 Skill 根相对解析；可重复的确定性操作进入 `scripts/`；`tests/` 至少覆盖入口、路径/资源解析和关键失败边界。模板作为资源时放在 `assets/templates/`，不再建立并列的顶层 `templates/`。所有项目资产从 `{PLANNING_ROOT}` / `{ENGINE_ROOT}` 的索引或仓库相对路径解析，新过程文件从 `{WORK_ROOT}` 的受管 job 解析。现行指令、示例命令和可执行脚本不得包含固定盘符、用户名或个人机器绝对路径；历史路径只可在同一行明确标为 provenance-only 且不可执行。

每个新 Skill 在注册表声明 `layout_schema: ndc-skill-layout/v1`。现存 Skill 列入 2026-09-11 grandfathered 清单，不因本规则批量补空目录；但以后改到某类资源时必须放入对应目录，并继续消除该 Skill 的现行机器路径。`scripts/art_pipeline/audit_skill_bundles.py` 分别检查执行脚本、现行文档、相对链接、已登记资源、测试夹具提示和两 checkout 的本机配置一致性。

跨设备脚本统一从仓库根运行 `python -B scripts/art_pipeline/ndc_art.py run <skill-name> <script-name> -- <args>`。公共入口根据注册表找到主 Skill，Python 使用当前入口解释器，Node 依次读取 `NDC_NODE_EXE`、PATH 及当前用户 Codex 内置运行时。本机外部程序位置、仓库根和状态目录只允许放在被 Git 忽略的配置或环境变量；若 Skill 自有脚本、同级校验器或资源缺失，应报告部署不完整并补交整个 Skill，禁止回另一台电脑的绝对路径读取。

UI 肖像独立归类为“静态美术 → 角色 → UI 肖像”，入口为 `.codex/skills/ndc-generate-ui-portraits/SKILL.md`。它使用已批准角色卡和通用肖像生成带场景的 3:4 胸像母图，分别裁切为 big 222×240 与 small 106×120，不接管通用角色设计、表情库或 H3 视频。目录保持现有扁平发现结构，分类记录在 Skill 元数据与上述注册表中。

工程侧视频链为：`runninghub-h3-greenscreen-avatar` 读取正式 Talk/Voice 上下文、生成并审查候选 → `runninghub-h3-ref2va-audio` 执行当前固定应用的上传、调用和下载 → 用户确认后由 `ndc-h3-avatar-delivery` 准备视频交付。最后一步的工具只处理受管任务中的 staging/ready，实际写入正式工程目标仍按已经授权的独立交付步骤执行。

`runninghub-h3-ref2va` 旧接口、`simplify-ndc-emergency-art`、`ndc-storyboard-to-seedance` 已按用户指令退出使用。迁移备份和历史任务日志保留，不把备份当可用入口；正在等待确认的视频任务不迁位、不清理。

共享 Python 路径接口是策划库 `scripts/art_pipeline/art_paths.py` 的 `load_art_paths()`；位置按首次配置章节解析。`skill_sources.json` 只登记维护归属与仓库内相对路径，视频 Skill 回工程不改变角色参考来源或项目外生产路径。

美术同学正在修改道具主 Skill。这次整理不改以下四个目录，不改其锁定提示词、生成逻辑或图片规格：

- `ndc-scene-evidence-placement`
- `ndc-evidence-scene-placement`
- `ndc-evidence-container`
- `ndc-evidence-detail-art`

道具新任务读取主 Skill。三个旧子流程与 `51ca823` 的参数/交付合同未同步，只作为历史参考，不直接执行其旧打包命令。兼容问题由 `production/art_pipeline/evidence_compatibility.json` 单独登记，待同事版本稳定后再定向修订。保留文件不会掩盖执行不兼容。

本机公共路径及用户确认规则以本文为准：道具 Skill 中旧 `D:/Codex/NDC/工作过程文件`、`最终交付` 都映射为当前任务 `payload` 下的过程/待确认子目录；不得因此直接写入工程或改动艺术要求。旧 `D:/PMH` 素材来源必须重新核实实际批准素材，不能用名字近似的图冒充。

道具 `evidence_delivery.py` 的旧 Unity 禁写检查只识别原机器路径，没有 `--unity-root` 参数。新任务必须经公共入口运行其 `package` 命令，公共入口校验 `--output-dir` 位于现存受管任务的 `payload` 内，弥补旧检查在换电脑后的缺口；不要直接照抄旧文档运行该脚本。脚本自带的锁定模板和遮罩仍从 Git 内的主 Skill `assets/` 读取；`--folder-path` 是运行时资源相对地址，不是写盘目标。

```powershell
python -B scripts/art_pipeline/ndc_art.py run ndc-scene-evidence-placement evidence_delivery.py package --help
python -B scripts/art_pipeline/audit_skill_bundles.py
python -B scripts/art_pipeline/ndc_art.py tool stage --help
python -B scripts/art_pipeline/ndc_art.py tool texture --help
python -B scripts/art_pipeline/ndc_art.py tool final --help
```

三个外部校验器已在本库按现行记录合同重建，入口对应如下。它们验证已有视觉审核记录，不代替视觉检查，也不把旧记录自动升级为 PASS。

| 旧外部脚本名 | 本库入口 |
|---|---|
| `validate-ndc-stage-visual-self-check.py` | `scripts/art_pipeline/validate_stage_visual_self_check.py` |
| `validate-ndc-texture-gate.py` | `scripts/art_pipeline/validate_texture_gate.py` |
| `validate-ndc-final-visual-record-presence.py` | `scripts/art_pipeline/validate_final_visual_record_presence.py` |

具体支持字段、错误和兼容范围见 `production/art_pipeline/validator_contracts.md`。不支持的旧记录必须补真实审核证据，不得删除门禁或伪造批准。小型审核记录可留在 job 的记录区；大幅审核截图与过程图在确认交付后的关闭阶段允许删除，记录中注明已按收尾策略清理。日后返工重新审查，不能把已不存在的截图当当前视觉凭据。

在拉取或整合远程前运行 `scripts/art_pipeline/check_upstream.py --fetch`，它报告同事新增修改、受保护文件的本地改动和潜在重叠；不会自动合并、提交、推送或覆盖文件。
