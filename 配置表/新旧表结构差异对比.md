# 新旧表结构差异对比文档

## 📋 文档说明

本文档详细对比了新旧两版配置表结构的差异，帮助团队理解设计变化、迁移成本和实施方案。

**版本对比**：
- 旧版（v1.0）：当前使用的表结构
- 新版（v2.0）：PDF文档中的新设计

**对比日期**：2025-11-12

---

## 1. 整体设计理念对比

### 1.1 核心差异

| 维度 | 旧版设计 | 新版设计 |
|------|---------|---------|
| **设计理念** | 数据与逻辑混合 | 数据与逻辑分离 |
| **职责范围** | 配置表包含游戏逻辑 | 配置表只存储静态展示数据 |
| **国际化** | 仅中文 | 完整双语支持（中英文） |
| **运行时状态** | 包含运行时状态字段 | 纯静态数据，不含运行时状态 |
| **ID命名** | 字符串ID，无统一规范 | 数字ID，有明确命名规则 |
| **扩展性** | 添加功能需改表结构 | 通过代码扩展，表结构稳定 |

### 1.2 设计哲学转变

**旧版：配置驱动**
```
配置表 = 数据 + 逻辑 + 状态
优点：快速开发，一目了然
缺点：耦合严重，难以维护
```

**新版：数据驱动**
```
配置表 = 纯静态数据
逻辑 = 代码实现
状态 = 运行时管理
优点：职责清晰，易于维护
缺点：需要更多代码支持
```

---

## 2. Talk表（对话表）差异详解

### 2.1 字段对比

| 功能 | 旧版字段 | 新版字段 | 变化说明 |
|------|---------|---------|----------|
| **唯一标识** | DialogueID (string)<br>如：D001, D002 | id (int)<br>如：001001001 | ✅ 改为9位数字，规范化命名<br>前3位=人物，中3位=第几次对话，后3位=第几句 |
| **步骤序号** | ❌ 无 | step (int)<br>如：1, 2, 3 | ✅ 新增，方便查看和编辑 |
| **对话类型** | DialogueType (string)<br>如：AVG | speakType (int)<br>1=旁白, 2=人物, 3=空白 | ✅ 改为数字枚举，更精确 |
| **场景ID** | SceneID (string)<br>如：SC101 | ❌ 移除 | ⚠️ 场景关联由代码管理 |
| **NPC ID** | NPCID (string)<br>如：Morrison | ❌ 移除 | ⚠️ 场景NPC由代码管理 |
| **说话人** | NPCTalk (string)<br>如：NPC004 | cnSpeaker (string)<br>enSpeaker (string) | ✅ 直接存储说话人名字，支持双语 |
| **等待时间** | ❌ 无 | waitTime (float) | ✅ 新增，控制对话节奏 |
| **头像** | Icon, CharacterPortrait | imagePath (string) | ✅ 统一为资源路径 |
| **语音** | VoiceFile | voicePath (string) | ✅ 重命名，更明确 |
| **对话内容** | Content (string)<br>仅中文 | cnWords (string)<br>enWords (string) | ✅ 完整双语支持 |
| **前置对话** | frontContent (string) | ❌ 移除 | ⚠️ 不需要双向指针 |
| **下一句** | NextContent (string)<br>如：D002 | next (string)<br>如：001001002 | ✅ 支持分支：用/分隔多个ID<br>如：001001009/001001011/001001013 |
| **选项** | Options (string)<br>如：D003,D004,D005 | ❌ 移除，整合到next | ✅ 与next字段配合使用 |
| **是否结束** | IsEnd (string)<br>0或1 | next = 0 或 script = "end" | ✅ 通过next和script控制 |
| **触发事件** | TriggerEvent (string) | script (string)<br>Parameter0-3 | ✅ 统一的脚本系统 |
| **提取证词** | ExtractHighlight (string) | ❌ 移除 | ⚠️ 证词管理由代码实现 |

### 2.2 新增脚本系统

**旧版**：只有简单的事件触发
```json
{
  "TriggerEvent": "EV007_Collected"
}
```

**新版**：完整的脚本系统
```json
{
  "script": "audio",           // audio, shake, branches, end
  "Parameter0": "TalkAudio001", // 参数1
  "Parameter1": "",             // 参数2
  "Parameter2": "",             // 参数3
  "Parameter3": ""              // 参数4
}
```

支持的脚本类型：
- `audio`：播放音效（参数0=路径）
- `shake`：相机震动（参数0=强度）
- `branches`：选项分支（参数0-3=选项文本）
- `end`：对话结束

### 2.3 分支系统改进

**旧版**：分离的字段
```json
{
  "DialogueID": "D002",
  "Content": "",
  "Options": "D003,D004,D005",
  "NextContent": ""
}
```

**新版**：整合的设计
```json
{
  "id": 001001008,
  "cnWords": "选择一个选项：",
  "next": "001001009/001001011/001001013",
  "script": "branches",
  "Parameter0": "选项A",
  "Parameter1": "选项B",
  "Parameter2": "选项C",
  "Parameter3": ""
}
```

### 2.4 ID命名规则对比

**旧版**：
```
D001, D002, D003...
- 无规律
- 无法从ID看出对话归属
- 重构时难以批量修改
```

**新版**：
```
001001001 = 人物001的第001次对话的第001句
001001002 = 人物001的第001次对话的第002句
001002001 = 人物001的第002次对话的第001句
210001    = 系统旁白001
220001    = 其他角色001

优点：
- 结构化，一目了然
- 便于批量管理
- 支持按人物、对话批次查询
```

### 2.5 数据示例对比

**旧版数据**：
```json
{
  "DialogueID": "D001",
  "SceneID": "SC101",
  "NPCID": "Morrison",
  "DialogueType": "AVG",
  "Icon": "",
  "CharacterPortrait": "",
  "FrontContent": "",
  "NPCTalk": "NPC004",
  "Content": "芝加哥警局！放下武器！Webb死了，你就是凶手！",
  "Options": "",
  "IsEnd": "0",
  "VoiceFile": "",
  "ExtractHighlight": "",
  "NextContent": "D002",
  "TriggerEvent": ""
}
```

**新版数据**：
```json
{
  "id": 210001,
  "step": 1,
  "speakType": 2,
  "waitTime": 3,
  "cnSpeaker": "Morrison",
  "enSpeaker": "Morrison",
  "imagePath": "Art/UI/Character/Morrison",
  "voicePath": "Audio/Voice/Morrison01.mp3",
  "cnWords": "芝加哥警局！放下武器！Webb死了，你就是凶手！",
  "enWords": "Chicago Police! Drop your weapon! Webb is dead, and you're the killer!",
  "next": "210002",
  "script": "audio",
  "Parameter0": "TalkAudio001",
  "Parameter1": "",
  "Parameter2": "",
  "Parameter3": ""
}
```

**对比分析**：
- ✅ 新版更清晰：字段命名更直观
- ✅ 新版支持双语：完整的中英文
- ✅ 新版更强大：脚本系统支持更多功能
- ⚠️ 新版字段更多：但职责更明确

---

## 3. NPC/Role表差异详解

### 3.1 表名变化

| 项目 | 旧版 | 新版 |
|------|------|------|
| **表名** | Role | NPCStaticData |
| **数据类名** | NDCRoleData | （待定义） |
| **文件名** | Role.json | NPCStaticData.json |

### 3.2 字段对比

| 功能 | 旧版字段 | 新版字段 | 变化说明 |
|------|---------|---------|----------|
| **唯一标识** | NPCID (string)<br>如：NPC001, NPC002 | id (string)<br>如：百田解斗, 赤松枫 | ✅ 直接使用角色名作为ID |
| **角色名** | NPCName (string)<br>仅中文 | cnName (string)<br>enName (string) | ✅ 完整双语支持 |
| **头像** | RoleIcon (string)<br>Portrait (string) | path1/2/3 (string) | ✅ 统一为资源路径，支持多个 |
| **背景描述** | BgDescription (string)<br>仅中文 | cnDescribe1/2/3<br>enDescribe1/2/3 | ✅ 双语支持，可多个描述 |
| **证词** | ❌ 无 | cnTestimony1/2/3<br>enTestimony1/2/3 | ✅ 新增证词字段 |
| **解锁条件** | UnlockCondition (string)<br>如：EVE001 | ❌ 移除 | ⚠️ 由代码管理 |
| **选中状态** | SelectedState (int)<br>0或1 | ❌ 移除 | ⚠️ 运行时状态，不应在配置表 |
| **标记状态** | Marked (int)<br>0或1 | ❌ 移除 | ⚠️ 运行时状态，不应在配置表 |
| **对话内容** | DialogueContent (string)<br>如：DLG001,DLG003 | ❌ 移除 | ⚠️ 关联关系由代码管理 |
| **指证内容** | ExposeMessage (string)<br>如：DAT002,DAT003 | ❌ 移除 | ⚠️ 关联关系由代码管理 |

### 3.3 设计思路变化

**旧版：大而全**
```
角色表 = 基础信息 + 资源路径 + 解锁逻辑 + 运行时状态 + 关联数据
- 一个表包含所有角色相关信息
- 配置表驱动游戏逻辑
- 难以维护和扩展
```

**新版：职责单一**
```
角色表 = 基础信息 + 资源路径 + 描述文本 + 证词文本
- 只存储静态展示数据
- 逻辑由代码管理
- 清晰易维护
```

### 3.4 移除字段的处理方案

| 旧字段 | 新版处理方案 |
|--------|-------------|
| **UnlockCondition** | 在代码中定义解锁规则表<br>`Dictionary<string, UnlockCondition>` |
| **SelectedState** | 运行时状态，存储在SaveData中 |
| **Marked** | 运行时状态，存储在SaveData中 |
| **DialogueContent** | 通过代码关联：对话表中通过角色名查询 |
| **ExposeMessage** | 通过代码关联：指证表中通过NPCID查询 |

### 3.5 数据示例对比

**旧版数据**：
```json
{
  "NPCID": "NPC002",
  "NPCName": "Rosa",
  "RoleIcon": "",
  "Portrait": "note_rosa",
  "BgDescription": "50岁的清洁工",
  "UnlockCondition": "",
  "SelectedState": 0,
  "Marked": 1,
  "DialogueContent": "DLG001,DLG003",
  "ExposeMessage": "DAT002,DAT003,DAT004"
}
```

**新版数据**：
```json
{
  "id": "Rosa",
  "cnName": "Rosa Martinez",
  "enName": "Rosa Martinez",
  "path1": "Art/UI/Character/Rosa",
  "path2": "Art/UI/Character/Rosa_Portrait",
  "path3": "",
  "cnTestimony1": "当天晚上我被TOMMY经理安排了工作",
  "cnTestimony2": "于是我一直在地下室酒窖清理，整理酒瓶和架子",
  "cnTestimony3": "我没有听到任何的脚步声",
  "enTestimony1": "I was assigned work by Manager Tommy that night",
  "enTestimony2": "So I was cleaning in the basement wine cellar, organizing bottles",
  "enTestimony3": "I didn't hear any footsteps",
  "cnDescribe1": "50岁的清洁工，为了儿子的医疗费而努力工作",
  "cnDescribe2": "看起来很老实，但证词有疑点",
  "cnDescribe3": "",
  "enDescribe1": "50-year-old cleaner, working hard for her son's medical expenses",
  "enDescribe2": "Looks honest, but her testimony has doubts",
  "enDescribe3": ""
}
```

**对比分析**：
- ✅ 新版更纯净：只有静态数据
- ✅ 新版更丰富：支持双语和多描述
- ✅ 新版更灵活：证词独立管理
- ⚠️ 新版需要代码：关联关系和逻辑由代码实现

---

## 4. Evidence/Item表差异详解

### 4.1 表名变化

| 项目 | 旧版 | 新版 |
|------|------|------|
| **表名** | Evidence | ItemStaticData |
| **数据类名** | NDCEvidenceData | （待定义） |
| **文件名** | Evidence.json | ItemStaticData.json |

### 4.2 字段对比

| 功能 | 旧版字段 | 新版字段 | 变化说明 |
|------|---------|---------|----------|
| **唯一标识** | EvidenceID (string)<br>如：EV001, EV002 | id (string)<br>如：item001 | ✅ 更规范的命名 |
| **名称** | EvidenceName (string)<br>仅中文 | cnName (string)<br>enName (string) | ✅ 完整双语支持 |
| **类型** | EvidenceType (string)<br>item, clue, note | itemType (string)<br>item, clue, envir | ✅ 类型更明确<br>note改为envir（环境物品） |
| **图标** | Icon (string)<br>DetailImage (string) | path1/2/3 (string) | ✅ 统一为资源路径，支持多个 |
| **描述** | Description (string)<br>仅中文 | cnDescribe1/2/3<br>enDescribe1/2/3 | ✅ 双语支持，可多个描述 |
| **分析相关** | AnalysisRequired (string)<br>AnalysisResult (string) | cnDescribe2 | ✅ 整合：描述1=初始，描述2=分析后 |
| **相关NPC** | RelatedNPCIDs (string)<br>NPCName (string) | ❌ 移除 | ⚠️ 关联关系由代码管理 |
| **相关场景** | RelatedSceneID (string) | ❌ 移除 | ⚠️ 关联关系由代码管理 |
| **组合系统** | CombinatableWith (string)<br>CombineResult (string) | ❌ 移除 | ⚠️ 组合逻辑由代码实现 |
| **指证系统** | UsedInExposure (string)<br>ExposureTime (string) | ❌ 移除 | ⚠️ 指证逻辑由代码实现 |
| **触发事件** | TriggerEvent (string) | ❌ 移除 | ⚠️ 事件系统由代码实现 |
| **显示方式** | DisplayAfterGot (string) | ❌ 移除 | ⚠️ 显示逻辑由代码实现 |

### 4.3 移除字段数量统计

| 分类 | 旧版字段数 | 新版字段数 | 移除数量 |
|------|-----------|-----------|----------|
| **基础信息** | 5 | 4 | -1 |
| **描述信息** | 2 | 6 | +4（双语） |
| **资源路径** | 2 | 3 | +1 |
| **游戏逻辑** | 9 | 0 | -9 |
| **总计** | 18 | 13 | -5 |

### 4.4 类型定义变化

| 旧版类型 | 说明 | 新版类型 | 变化 |
|---------|------|---------|------|
| item | 物品 | item | ✅ 保持一致 |
| clue | 线索 | clue | ✅ 保持一致 |
| note | 笔记/证词 | envir | ⚠️ 改为环境物品（不可收集） |

**类型语义变化**：
- `note` 在旧版中表示证词类证据
- `envir` 在新版中表示环境物品（只能查看，不能收集）
- 证词功能移到NPC表中单独管理

### 4.5 多描述的使用策略

**旧版**：
```json
{
  "Description": "白色毛巾",
  "AnalysisRequired": "1",
  "AnalysisResult": "一条白色毛巾，散发着刺鼻的甜腻气味，毛巾上有明显的使用痕迹"
}
```

**新版**：
```json
{
  "cnDescribe1": "白色毛巾",
  "cnDescribe2": "一条白色毛巾，散发着刺鼻的甜腻气味，毛巾上有明显的使用痕迹",
  "cnDescribe3": "经过化验，毛巾上残留的是氯仿成分"
}
```

**新版优势**：
- 可以有多个状态的描述
- 不需要额外的逻辑字段
- 代码根据游戏进度选择显示哪个描述

### 4.6 移除字段的处理方案

| 旧字段 | 新版处理方案 |
|--------|-------------|
| **RelatedNPCIDs** | 代码中定义关联表<br>`Dictionary<string, List<string>>` |
| **RelatedSceneID** | 场景探索系统中配置 |
| **AnalysisRequired** | 通过代码判断：如果只有cnDescribe1有值，无需分析 |
| **CombinatableWith** | 代码中定义组合规则表 |
| **CombineResult** | 代码中定义组合结果 |
| **UsedInExposure** | 指证系统中配置 |
| **ExposureTime** | 指证系统中配置 |
| **TriggerEvent** | 事件系统中配置 |
| **DisplayAfterGot** | 探索系统中配置 |

### 4.7 数据示例对比

**旧版数据**：
```json
{
  "EvidenceID": "EV003",
  "EvidenceName": "毛巾",
  "EvidenceType": "item",
  "Icon": "towel",
  "DetailImage": "",
  "Description": "白色毛巾",
  "RelatedNPCIDs": "NPC002",
  "NPCName": "Rosa",
  "RelatedSceneID": "SC003",
  "AnalysisRequired": "1",
  "AnalysisResult": "一条白色毛巾，散发着刺鼻的甜腻气味，毛巾上有明显的使用痕迹",
  "CombinatableWith": "",
  "CombineResult": "",
  "UsedInExposure": "SEC01",
  "ExposureTime": "2",
  "TriggerEvent": "",
  "DisplayAfterGot": "clear"
}
```

**新版数据**：
```json
{
  "id": "item003",
  "cnName": "毛巾",
  "enName": "Towel",
  "itemType": "item",
  "cnDescribe1": "白色毛巾",
  "cnDescribe2": "一条白色毛巾，散发着刺鼻的甜腻气味，毛巾上有明显的使用痕迹",
  "cnDescribe3": "经过化验，毛巾上残留的是氯仿成分",
  "enDescribe1": "White towel",
  "enDescribe2": "A white towel with a pungent sweet smell and obvious signs of use",
  "enDescribe3": "Lab analysis shows chloroform residue on the towel",
  "path1": "Art/UI/Item/Towel",
  "path2": "Art/UI/Item/Towel_Detail",
  "path3": ""
}
```

**对比分析**：
- ✅ 新版更专注：只存储物品本身的信息
- ✅ 新版更丰富：支持多状态描述和双语
- ✅ 新版更灵活：逻辑变化不需要改表
- ⚠️ 新版需配套：需要代码系统支持逻辑

---

## 5. 表结构数量对比

### 5.1 表数量变化

| 类型 | 旧版 | 新版 | 变化 |
|------|------|------|------|
| **核心配置表** | 5张 | 3张 | -2 |
| **总字段数** | 约60个 | 约30个 | -30 |

**旧版5张表**：
1. Talk（对话表）
2. Role（角色表）
3. Evidence（证据表）
4. Task（任务表）
5. InvestigateExposeMessage（指证信息表）

**新版3张表**：
1. Talk（对话表）
2. NPCStaticData（角色静态数据表）
3. ItemStaticData（物品静态数据表）

**减少的表**：
- Task表：任务系统由代码管理
- InvestigateExposeMessage表：指证系统由代码管理

### 5.2 字段数量对比

#### Talk表
```
旧版：15个字段
新版：13个字段
减少：2个（frontContent, IsEnd）
新增：0个（但很多字段改名或类型变化）
```

#### Role/NPC表
```
旧版：10个字段
新版：13个字段（基础）+ 可扩展
减少：4个（游戏逻辑字段）
新增：7个（双语支持和证词）
```

#### Evidence/Item表
```
旧版：18个字段
新版：13个字段（基础）+ 可扩展
减少：9个（游戏逻辑字段）
新增：4个（双语支持）
```

---

## 6. 数据迁移方案

### 6.1 Talk表迁移

#### 步骤1：ID转换规则

**旧版ID → 新版ID映射**：
```
需要重新分配ID
建议：
- 根据NPC建立人物编号表
- 根据对话场景分配对话批次
- 按对话顺序分配句子编号

示例映射：
D001 → 210001 (Morrison的对话)
D002 → 220001 (Zack选择)
D003 → 220002 (Zack选项A)
```

#### 步骤2：字段映射

```python
# 伪代码
def migrate_talk(old_data):
    new_data = {
        'id': generate_new_id(old_data['NPCTalk']),
        'step': calculate_step(),
        'speakType': get_speak_type(old_data['DialogueType']),
        'waitTime': 3.0,  # 默认值
        'cnSpeaker': get_npc_name(old_data['NPCTalk']),
        'enSpeaker': translate_to_english(get_npc_name(old_data['NPCTalk'])),
        'imagePath': get_image_path(old_data['NPCTalk']),
        'voicePath': old_data['VoiceFile'],
        'cnWords': old_data['Content'],
        'enWords': translate_to_english(old_data['Content']),
        'next': convert_next_id(old_data['NextContent'], old_data['Options']),
        'script': determine_script_type(old_data),
        'Parameter0': extract_parameter(old_data, 0),
        'Parameter1': extract_parameter(old_data, 1),
        'Parameter2': extract_parameter(old_data, 2),
        'Parameter3': extract_parameter(old_data, 3),
    }
    return new_data
```

#### 步骤3：分支处理

```python
def convert_next_id(next_content, options):
    if options:
        # 有选项，转换为分支格式
        option_ids = options.split(',')
        return '/'.join([convert_id(id) for id in option_ids])
    elif next_content:
        return convert_id(next_content)
    else:
        return '0'  # 结束
```

#### 步骤4：脚本转换

```python
def determine_script_type(old_data):
    if old_data['Options']:
        return 'branches'
    elif old_data['IsEnd'] == '1':
        return 'end'
    elif old_data['TriggerEvent']:
        return 'audio'  # 需要根据事件类型判断
    else:
        return ''
```

### 6.2 NPC表迁移

#### 字段映射
```python
def migrate_npc(old_data):
    new_data = {
        'id': old_data['NPCName'],  # 使用名字作为ID
        'cnName': old_data['NPCName'],
        'enName': translate_to_english(old_data['NPCName']),
        'path1': convert_portrait_path(old_data['Portrait']),
        'path2': '',
        'path3': '',
        # 证词需要从对话表或指证表中提取
        'cnTestimony1': extract_testimony(old_data['NPCID'], 1),
        'cnTestimony2': extract_testimony(old_data['NPCID'], 2),
        'cnTestimony3': extract_testimony(old_data['NPCID'], 3),
        'enTestimony1': translate_testimony(1),
        'enTestimony2': translate_testimony(2),
        'enTestimony3': translate_testimony(3),
        'cnDescribe1': old_data['BgDescription'],
        'cnDescribe2': '',
        'cnDescribe3': '',
        'enDescribe1': translate_to_english(old_data['BgDescription']),
        'enDescribe2': '',
        'enDescribe3': '',
    }
    return new_data
```

#### 逻辑迁移到代码
```csharp
// 原来在配置表中的逻辑
public class NPCUnlockSystem
{
    private Dictionary<string, string> unlockConditions = new()
    {
        { "Tommy", "EVE001" },
        { "Morrison", "EVE002" },
    };

    private Dictionary<string, List<string>> dialogueMapping = new()
    {
        { "Rosa", new List<string> { "DLG001", "DLG003" } },
        { "Tommy", new List<string> { "DLG004", "DLG005" } },
    };

    public bool IsUnlocked(string npcId)
    {
        if (!unlockConditions.ContainsKey(npcId)) return true;
        return EventSystem.IsTriggered(unlockConditions[npcId]);
    }
}
```

### 6.3 Item表迁移

#### 字段映射
```python
def migrate_item(old_data):
    new_data = {
        'id': convert_item_id(old_data['EvidenceID']),
        'cnName': old_data['EvidenceName'],
        'enName': translate_to_english(old_data['EvidenceName']),
        'itemType': old_data['EvidenceType'],
        'cnDescribe1': old_data['Description'],
        'cnDescribe2': old_data['AnalysisResult'] if old_data['AnalysisRequired'] == '1' else '',
        'cnDescribe3': '',
        'enDescribe1': translate_to_english(old_data['Description']),
        'enDescribe2': translate_to_english(old_data['AnalysisResult']),
        'enDescribe3': '',
        'path1': convert_icon_path(old_data['Icon']),
        'path2': old_data['DetailImage'],
        'path3': '',
    }
    return new_data
```

#### 逻辑迁移到代码
```csharp
// 证据关联系统
public class EvidenceRelationSystem
{
    // 证据与NPC的关联
    private Dictionary<string, List<string>> evidenceToNPC = new()
    {
        { "item001", new List<string> { "Rosa" } },
        { "item002", new List<string> { "Rosa" } },
    };

    // 证据与场景的关联
    private Dictionary<string, string> evidenceToScene = new()
    {
        { "item001", "SC003" },
        { "item002", "SC004" },
    };

    // 证据组合规则
    private Dictionary<string, Dictionary<string, string>> combineRules = new()
    {
        { "item002", new Dictionary<string, string>
            {
                { "item003", "item_combined_001" }
            }
        }
    };

    // 指证使用规则
    private Dictionary<string, ExposureRule> exposureRules = new()
    {
        { "item001", new ExposureRule("SEC01", 1) },
        { "item003", new ExposureRule("SEC01", 2) },
    };
}
```

### 6.4 迁移工具伪代码

```python
class ConfigMigrationTool:
    def __init__(self, old_data_path, new_data_path):
        self.old_data_path = old_data_path
        self.new_data_path = new_data_path
        self.id_mapping = {}  # 旧ID到新ID的映射表

    def migrate_all(self):
        # 1. 迁移Talk表
        self.migrate_talk_table()

        # 2. 迁移NPC表
        self.migrate_npc_table()

        # 3. 迁移Item表
        self.migrate_item_table()

        # 4. 生成代码配置类
        self.generate_logic_code()

        # 5. 验证数据完整性
        self.validate_migration()

    def validate_migration(self):
        # 检查ID引用完整性
        # 检查必填字段
        # 检查数据一致性
        pass
```

---

## 7. 迁移成本评估

### 7.1 工作量估算

| 任务 | 预计工时 | 难度 | 风险 |
|------|---------|------|------|
| **Talk表迁移** | 3天 | 高 | 高 |
| - ID重新分配 | 1天 | 中 | 中 |
| - 字段映射和转换 | 1天 | 高 | 高 |
| - 分支系统调整 | 0.5天 | 中 | 中 |
| - 翻译英文内容 | 0.5天 | 低 | 低 |
| **NPC表迁移** | 1.5天 | 中 | 中 |
| - 字段映射 | 0.5天 | 低 | 低 |
| - 证词提取整理 | 0.5天 | 中 | 中 |
| - 翻译英文内容 | 0.5天 | 低 | 低 |
| **Item表迁移** | 2天 | 中 | 中 |
| - 字段映射 | 0.5天 | 低 | 低 |
| - 描述状态拆分 | 1天 | 中 | 中 |
| - 翻译英文内容 | 0.5天 | 低 | 低 |
| **代码改造** | 5天 | 高 | 高 |
| - 解锁系统重构 | 1天 | 中 | 中 |
| - 关联系统重构 | 1.5天 | 高 | 高 |
| - 组合系统重构 | 1天 | 中 | 中 |
| - 指证系统重构 | 1天 | 中 | 中 |
| - 探索系统重构 | 0.5天 | 中 | 中 |
| **测试验证** | 3天 | 中 | 高 |
| - 功能测试 | 2天 | 中 | 高 |
| - 回归测试 | 1天 | 中 | 高 |
| **总计** | 14.5天 | - | - |

### 7.2 技术难点

1. **Talk表ID重新分配**
   - 需要建立旧ID到新ID的映射表
   - 所有引用Talk ID的地方都要更新
   - 分支跳转关系要保持正确

2. **对话分支系统改造**
   - 旧版：Options + NextContent
   - 新版：next字段统一处理
   - 需要仔细处理分支逻辑

3. **游戏逻辑迁移到代码**
   - 原本在配置表中的逻辑要用代码实现
   - 需要设计新的系统架构
   - 要保证功能一致性

4. **双语翻译**
   - 所有文本内容需要英文翻译
   - 保持语义准确性
   - 工作量较大

5. **数据完整性验证**
   - ID引用完整性检查
   - 分支跳转正确性检查
   - 资源路径有效性检查

### 7.3 风险评估

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| **ID映射错误** | 高 | 高 | 编写自动化验证工具 |
| **分支逻辑错误** | 中 | 高 | 详细测试每个对话分支 |
| **翻译质量问题** | 中 | 中 | 专业翻译审核 |
| **代码逻辑bug** | 中 | 高 | 单元测试 + 集成测试 |
| **性能下降** | 低 | 中 | 性能测试对比 |
| **旧存档不兼容** | 高 | 高 | 编写存档转换工具 |

---

## 8. 分阶段实施方案

### 8.1 阶段一：准备阶段（2天）

**目标**：环境准备和工具开发

**任务**：
1. ✅ 编写新旧表结构对比文档（本文档）
2. ✅ 编写数据迁移工具
3. ✅ 准备测试环境
4. ✅ 建立ID映射规则表

**产出**：
- 迁移工具
- 测试环境
- ID映射表

### 8.2 阶段二：数据迁移（6.5天）

**目标**：完成配置表数据迁移

**任务**：
1. 迁移Talk表（3天）
2. 迁移NPC表（1.5天）
3. 迁移Item表（2天）

**产出**：
- 新版JSON配置文件
- 迁移报告

### 8.3 阶段三：代码改造（5天）

**目标**：实现游戏逻辑系统

**任务**：
1. 解锁系统重构（1天）
2. 关联系统重构（1.5天）
3. 组合系统重构（1天）
4. 指证系统重构（1天）
5. 探索系统重构（0.5天）

**产出**：
- 新的系统代码
- 单元测试

### 8.4 阶段四：测试验证（3天）

**目标**：确保功能正常

**任务**：
1. 功能测试（2天）
2. 回归测试（1天）

**产出**：
- 测试报告
- Bug修复

### 8.5 阶段五：上线准备（1天）

**目标**：准备生产环境

**任务**：
1. 存档转换工具开发
2. 热更新准备
3. 回滚方案准备

**产出**：
- 上线清单
- 回滚方案

---

## 9. 优缺点对比总结

### 9.1 新版优势

| 优势 | 说明 | 影响 |
|------|------|------|
| **职责清晰** | 数据与逻辑分离，配置表只存储静态数据 | ⭐⭐⭐⭐⭐ 维护性大幅提升 |
| **国际化友好** | 完整的双语支持，易于扩展多语言 | ⭐⭐⭐⭐⭐ 方便国际化 |
| **可维护性强** | 逻辑变化不需要改表，改代码即可 | ⭐⭐⭐⭐⭐ 开发效率提升 |
| **扩展性好** | 字段可自由扩展，不影响现有逻辑 | ⭐⭐⭐⭐ 应对需求变化 |
| **结构清晰** | ID命名规范，表结构简洁 | ⭐⭐⭐⭐ 易于理解 |
| **类型安全** | 数字字段使用int而非string | ⭐⭐⭐ 减少类型错误 |

### 9.2 新版劣势

| 劣势 | 说明 | 应对方案 |
|------|------|---------|
| **迁移成本高** | 需要重新整理数据和改造代码 | 分阶段实施，降低风险 |
| **需要代码支持** | 很多逻辑需要在代码中实现 | 提供完善的系统架构 |
| **学习成本** | 团队需要适应新的设计理念 | 编写详细文档和示例 |
| **初期开发慢** | 需要先搭建代码框架 | 提供代码模板 |

### 9.3 旧版优势

| 优势 | 说明 |
|------|------|
| **快速开发** | 不需要写太多代码，配置即可 |
| **直观可见** | 所有逻辑在表中，一目了然 |
| **无需迁移** | 当前就在使用，无迁移成本 |

### 9.4 旧版劣势

| 劣势 | 说明 |
|------|------|
| **耦合严重** | 数据和逻辑混合，难以维护 |
| **缺乏国际化** | 只有中文，国际化困难 |
| **类型不安全** | 大量string字段存储数字/布尔值 |
| **扩展困难** | 添加功能需要改表结构 |
| **逻辑固化** | 游戏逻辑写死在配置中 |

---

## 10. 推荐方案

### 10.1 建议采用新版

**理由**：
1. ✅ 项目还在开发阶段，现在迁移成本相对较低
2. ✅ 长远来看维护成本大幅降低
3. ✅ 支持国际化，未来可以拓展海外市场
4. ✅ 架构清晰，利于团队协作
5. ✅ 更易于后续功能扩展

**前提条件**：
- 有2-3周的开发时间
- 团队对新架构有一定理解
- 有完善的测试计划

### 10.2 替代方案：渐进式迁移

如果时间紧张，可以考虑渐进式迁移：

**阶段1：双语支持**（1周）
- 保持旧表结构
- 添加英文字段
- 翻译所有文本内容

**阶段2：逻辑分离**（2周）
- 将游戏逻辑迁移到代码
- 保持旧ID不变
- 逐步清理配置表中的逻辑字段

**阶段3：表结构优化**（2周）
- 重新设计ID命名规则
- 优化表结构
- 完成完整迁移

**优点**：
- 风险可控
- 可以边开发边迁移
- 每个阶段都有产出

**缺点**：
- 总时间更长
- 过渡期维护成本高

---

## 11. 关键决策点

### 11.1 是否迁移？

**迁移**（推荐）：
- ✅ 适合长期项目
- ✅ 计划国际化
- ✅ 有足够的开发时间
- ✅ 追求代码质量

**不迁移**：
- ⚠️ 项目即将上线
- ⚠️ 团队人手不足
- ⚠️ 只做国内市场
- ⚠️ 快速迭代原型阶段

### 11.2 何时迁移？

**最佳时机**：
- 现在（开发阶段）
- 版本迭代的空档期
- 大版本更新前

**不推荐时机**：
- 临近上线
- 高强度开发期
- 人员流动期

### 11.3 谁来负责？

**建议团队配置**：
- 主程序员：1人（负责代码架构）
- 配置员：1人（负责数据迁移）
- 测试：1人（负责功能验证）
- 项目管理：1人（负责进度把控）

---

## 附录

### A. ID映射参考表

#### A.1 对话ID映射示例

| 旧ID | 角色 | 场景 | 新ID | 说明 |
|------|------|------|------|------|
| D001 | Morrison | SC101 | 210001 | 系统对话001 |
| D002 | Zack | SC101 | 220001 | 主角选择001 |
| D003 | Zack | SC101 | 220002 | 选项A |
| D004 | Zack | SC101 | 220003 | 选项B |

#### A.2 人物编号分配

| 编号 | 角色名 | 说明 |
|------|--------|------|
| 001 | 百田解斗 | 新表中的角色 |
| 002 | 赤松枫 | 新表中的角色 |
| 003 | 最原终一 | 新表中的角色 |
| 210 | 系统/旁白 | 系统提示 |
| 220 | 主角/通用 | Zack等 |
| 201 | Webb | 受害者 |
| 202 | Rosa | 嫌疑人1 |
| 203 | Tommy | 嫌疑人2 |
| 204 | Morrison | 嫌疑人3 |

### B. 迁移检查清单

#### B.1 数据迁移检查

- [ ] Talk表所有对话都已迁移
- [ ] 对话ID映射表完整
- [ ] 分支跳转关系正确
- [ ] 所有文本都有英文翻译
- [ ] NPC表所有角色都已迁移
- [ ] 证词信息已提取整理
- [ ] Item表所有物品都已迁移
- [ ] 多状态描述已拆分

#### B.2 代码改造检查

- [ ] 解锁系统已实现
- [ ] 关联系统已实现
- [ ] 组合系统已实现
- [ ] 指证系统已实现
- [ ] 探索系统已实现
- [ ] 单元测试已编写
- [ ] 集成测试已通过

#### B.3 功能验证检查

- [ ] 对话流程正常
- [ ] 分支选择正常
- [ ] NPC解锁正常
- [ ] 证据收集正常
- [ ] 证据分析正常
- [ ] 证据组合正常
- [ ] 指证系统正常
- [ ] 多语言切换正常

### C. 联系方式

如有疑问，请联系：
- 技术负责人：[待填写]
- 配置负责人：[待填写]
- 测试负责人：[待填写]

---

**文档结束**
