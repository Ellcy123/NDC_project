interface:
  display_name: "NDC Coordinate Image Edit"
  short_description: "Coordinate-locked edits with zero outside-mask drift."
  default_prompt: "Use $ndc-coordinate-image-edit to make a non-destructive local repair while preserving every pixel outside the approved region."
