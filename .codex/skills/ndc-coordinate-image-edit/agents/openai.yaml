interface:
  display_name: "NDC Coordinate Image Edit"
  short_description: "Small-mask raster repair with seam and union audits."
  default_prompt: "Use $ndc-coordinate-image-edit to make a small-mask NDC raster repair, scan every structural seam, and prove zero drift outside the union of approved masks."
