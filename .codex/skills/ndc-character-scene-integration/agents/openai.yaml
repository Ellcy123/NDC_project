interface:
  display_name: "NDC 角色融入场景"
  short_description: "从剧情时间轴与场景可供性自动调度角色，再校准入景交付"
  default_prompt: "Use $ndc-character-scene-integration to derive the runtime cast timeline, direct reproducible blocking against scene affordances and real UI masks, then place and verify approved NDC characters in the fixed scene."
policy:
  allow_implicit_invocation: true
