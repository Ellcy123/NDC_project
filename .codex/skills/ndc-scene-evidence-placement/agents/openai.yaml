interface:
  display_name: "NDC Evidence Production Router"
  short_description: "Route NDC evidence art to the correct production workflow."
  default_prompt: "Use $ndc-scene-evidence-placement to classify how each NDC record is acquired, invoke the correct child skills, and verify the staged delivery."
policy:
  allow_implicit_invocation: true
