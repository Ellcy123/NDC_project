interface:
  display_name: "NDC Evidence Scene Placement"
  short_description: "Place direct scene evidence with exact Map coordinates."
  default_prompt: "Use $ndc-evidence-scene-placement with an approved parent route contract to place or extract a direct NDC scene hotspot and verify its exact Map and Position package."

policy:
  allow_implicit_invocation: false
