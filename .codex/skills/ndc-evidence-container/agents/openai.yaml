interface:
  display_name: "NDC Evidence Container"
  short_description: "Build verified Type 6-to-7 container pickup chains."
  default_prompt: "Use $ndc-evidence-container to execute an already-routed Type 6-to-Type 7 container chain and package every individually clickable child."

policy:
  allow_implicit_invocation: false
