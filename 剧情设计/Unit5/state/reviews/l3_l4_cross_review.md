# Unit5 L3／L4 State 交叉审查

日期：2026-09-14。审查者负责 L1／L2 生成，本次仅审 L3／L4，不修改被审 State。依据为主大纲、两阶段剖面详案、统一来源与 State 合同及三份前检。已完整阅读被审两份 YAML，并逐条对照自由 NPC、白色证言、原物与强制事件。

当前结论：PASS（设计期交叉审查）。发现项均已由原作者修订并经本审查者回读关闭；L5 文件落盘后的最终跨轮根入口由主审汇总验证。设计期通过不等于 Unity 已能运行。

## 已核对通过

| 维度 | L3 | L4 |
|---|---|---|
| 普通 NPC 与证言 | Vivian 上方声音原文；Lawson 硫酸铜原文；L1 5061001 原记录回查不再发放；获批 Moore 问话仅取得 R1 | Foster 安眠药痕迹、管家两片蓝药、Mary 20:30 亲送且未转手原句完整；普通 Mary 原句未被扩写成现场完整否认 |
| 取证 | 测高、登记册、Moore 房间和岔口材料均为搜证；未因 NPC 邻近改成对白交付 | 录音室、护士站、书房、文件柜与护工房材料均为搜证；环境对象保持不入包，乙醚仅护士站 |
| 疑点与指证 | 5301—5303 各一至两件；M-7 沿用 L1 5106；湿痕与脚印留 L5 挂载 | 5401—5403 各两件；缺页环境归 5402；三轮依次换药、取页、身份，没有药性鉴定 |
| 特殊门控 | 第一次当前场景弹图，9−3−4＝2；第二次圆桌定位；不生成复原证据，推门是 AVG | R2 后 Mickey 推理再进身份轮；三材料洗清 Emma 为独立后置事件，没有造三条件疑点 |
| 知识与人物 | 初始事实加 L1/L2 outcomes；不揭示后续药物、钥石、隐藏室；同段开启书房端不洗清 Moore | 初始事实加前三轮 outcomes；开篇才披露远端安排；无寻找母盘目标；Moore 不离看守房，Pierce 承担两次反咬 |
| 事件顺序 | 两次图纸→实地推门→岔口→普通 R1→三轮→同段书房端第二入口 | 押送隔离→Emma 披露→录音室抵达后自由询问→三轮及中间推理→药性争议→后台洗清 Emma→L5 |

## 修订记录

1. Moore、Mary 普通对象的 `player_inquiry` 已改为 null，动因另存；复核关闭。
2. L3 书房端移动原先只有自然语言 movement；现已写明同一 AVG 的 `change_scene`、后续 segment、跳过 firstEnter 与正式 next 映射，复核关闭。
3. L3／L4 局部流程 flags 已补默认值、setter 与消费者；L3 → L4 已补真实 opening Talk 名，复核关闭。
4. L4 五件物证来源锚已校准：5405 → 大纲691、5406 → 692、5407 → 696、5408 → 697、5409 → 698；完整定义与 coverage 均已同步，复核关闭。内容本身正确，原记录偏一行导致串到相邻物件或空白。
5. L4 Mickey 推理在 R2 中途调用，现已改为 `active_talk: L4_expose_mary`；L3 第二次图纸也已改为 `active_talk` 加 `reached_segment: open_section_stage2`。两处不再以整段 Talk 已完成作为调用前提，复核关闭。
6. L4 跨轮出口正确保留于 `L4_clear_emma`，其 `next_talk` 已填 `L5_opening_corridor_release`；`expose.post_expose` 仍先接后台事件，没有跳过洗清 Emma。最终目标存在性留主审连同 L5 汇总检查。

## 后续制作限制

环境疑点、图纸中途恢复、Expose 内部选择及固定链需要已登记的 special_adapter。后续对白与配置不得擅自提前发放动态 R2/R3、把紫片定为安眠药或毒药、把三材料事件改为额外第四轮指证、让 Moore 回场指控 Lula，或用图纸直接确认暗门。上述能力尚未通过 Unity 实测，本报告不作运行验收。
