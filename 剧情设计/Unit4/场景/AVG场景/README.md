# Unit4 AVG 场景说明

- 这里收录开篇、转场、终幕，以及指证结束后不恢复自由探索的固定 AVG 演出；同一场景 ID 可以复用探索底图并增加演出状态。
- L2 Rosa 来访直接落入可探索的 SC4011，不另做重复 AVG 底图。
- SC4041 只作四十二层抵达建立镜头；现行 `init_scene` 已改为 4041，镜头结束后自动进入 4042，不设置可点击门或导航步骤。
- SC4029、SC4035 已作为短 AVG 节点写回 state；4029 后进入 4027，4035 后自动返回 4033。
- L1 保险柜开启使用 SC4002 指证后 AVG；L2 正式预审使用 SC4016 法庭 AVG；L3 Doris 指证后使用 SC4023 宅邸门口 AVG。
- L4 停止执行仍在对应探索场景内完成。
- 宅邸爆炸是挂在探索场景上的突发演出，不另建探索地点。
- 同一 state 节点含两套实际构图时分文件记录，例如 4043 的大楼撤离与档案车内；4044继续复用该车内构图并切换到拆阅状态。
